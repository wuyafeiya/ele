import defaultSettings from '@/settings'
const {sidebarLogo} =defaultSettings
const state = {
  sidebarLogo: sidebarLogo
}

const mutation = {
}

export default {
  namespaced: true,
  state,
  mutation,
  actions
}